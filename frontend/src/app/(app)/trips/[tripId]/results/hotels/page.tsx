import { ComingSoonCard } from "@/components/ui/ComingSoonCard";

export default function TripHotelsResultsPage() {
  return (
    <ComingSoonCard
      title="Hotel options"
      description="Hotels that fit your remaining budget, with deep links to book on the provider's own site, land here in Phase 6."
    />
  );
}

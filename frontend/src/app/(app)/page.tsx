import { ComingSoonCard } from "@/components/ui/ComingSoonCard";

export default function TripPlannerPage() {
  return (
    <div className="pt-2">
      <ComingSoonCard
        title="Plan a trip"
        description="The trip planner form — origin, destination, budget, and days — is built in Phase 4. Once it's live, submitting it here will kick off your places, transport, and hotel results."
      />
    </div>
  );
}

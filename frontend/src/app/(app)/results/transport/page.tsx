import { ComingSoonCard } from "@/components/ui/ComingSoonCard";

export default function TransportResultsPage() {
  return (
    <ComingSoonCard
      title="Transport options"
      description="Flight, train, and bus comparisons with deep links to book on the provider's own site land here in Phase 6."
    />
  );
}
